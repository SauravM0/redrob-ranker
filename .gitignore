.venv/
__pycache__/
*.pyc
# local tuning cache (regenerated by eval/pipeline.py) — NOT needed to reproduce the submission
artifacts/feature_cache.npz
# NOTE: artifacts/embeddings.npy (float16, ~77MB) IS committed when using the dense path,
# so a fresh clone reproduces the exact submission. Do not ignore it.
